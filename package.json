{
  "name": "puzzlecruise",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate:content": "node scripts/content-generator/auto-fetch-generate.js",
    "generate:wordle": "node scripts/content-generator/wordle-auto-fetch-generate.js",
    "generate:strands": "node scripts/content-generator/strands-auto-fetch-generate.js",
    "generate:pips": "node scripts/content-generator/pips-auto-fetch-generate.js",
    "generate:all": "npm run generate:content && npm run generate:wordle && npm run generate:strands && npm run generate:pips"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@google/generative-ai": "^0.24.1",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.7",
    "dotenv": "^17.4.2",
    "googleapis": "^173.0.0",
    "openai": "^6.39.0",
    "tailwindcss": "^4.3.0"
  }
}
